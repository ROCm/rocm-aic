# ROCm-ICMS LLM-D Deployment Infrastructure

# Default recipe - show available commands
default:
    @just --list

# Verify all prerequisites (tools and LLM-D cluster components)
verify-prereqs:
    @just setup/verify-prereqs

# Verify only tool prerequisites (kubectl, helm, helmfile, just, minikube)
verify-tools-prereqs:
    @just setup/verify-tools-prereqs

# Verify only LLM-D cluster prerequisites (Istio, monitoring, gateway provider, etc.)
verify-llm-d-prereqs:
    @just setup/verify-llm-d-prereqs
