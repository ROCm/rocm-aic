# Managed by Ansible. Export the server-owned cache filesystem over NFS/RDMA.
{{ nfs_rdma_export_path }}{% for client in nfs_rdma_export_clients %} {{ client }}({{ nfs_rdma_export_options }}){% endfor %}
