# Add vllm:model_weights_memory_bytes Prometheus gauge.
#
# Rebased for vLLM v0.27.1. Exposes the static GPU memory consumed by model
# weights so operators can distinguish it from KV-cache memory.
diff --git a/vllm/v1/engine/core.py b/vllm/v1/engine/core.py
index 11c78c7e74..a2f34398f4 100644
--- a/vllm/v1/engine/core.py
+++ b/vllm/v1/engine/core.py
@@ -296,6 +296,14 @@ class EngineCore:
             # Attention free models don't need memory for kv cache
             available_gpu_memory = [0] * len(kv_cache_specs)

+        # Model weights are static after model load, so retrieve this once
+        # after profiling and stamp it into each scheduler statistics update.
+        self._model_weights_memory_bytes: int = (
+            self.collective_rpc("get_model_weights_memory_bytes")[0]
+            if has_kv_cache
+            else 0
+        )
+
         assert len(kv_cache_specs) == len(available_gpu_memory)

         # Track max_model_len before KV cache config to detect auto-fit changes
@@ -559,6 +567,7 @@ class EngineCore:
     ) -> SchedulerStats:
         stats = self.scheduler.make_stats() or SchedulerStats()
         stats.iteration_details = iteration_details
+        stats.model_weights_memory_bytes = self._model_weights_memory_bytes
         return stats

     def _attach_iteration_details(
diff --git a/vllm/v1/metrics/loggers.py b/vllm/v1/metrics/loggers.py
index 692106cf39..7e22a8067d 100644
--- a/vllm/v1/metrics/loggers.py
+++ b/vllm/v1/metrics/loggers.py
@@ -568,6 +568,16 @@ class PrometheusStatLogger(AggregateStatLoggerBase):
             gauge_kv_cache_usage, per_engine_labelvalues
         )

+        gauge_model_weights_memory = self._gauge_cls(
+            name="vllm:model_weights_memory_bytes",
+            documentation="GPU memory used by model weights in bytes (static after load).",
+            multiprocess_mode="mostrecent",
+            labelnames=labelnames,
+        )
+        self.gauge_model_weights_memory = create_metric_per_engine(
+            gauge_model_weights_memory, per_engine_labelvalues
+        )
+
         if envs.VLLM_COMPUTE_NANS_IN_LOGITS:
             counter_corrupted_requests = self._counter_cls(
                 name="vllm:corrupted_requests",
@@ -1122,6 +1132,11 @@ class PrometheusStatLogger(AggregateStatLoggerBase):
             )
             self.gauge_kv_cache_usage[engine_idx].set(scheduler_stats.kv_cache_usage)

+            if scheduler_stats.model_weights_memory_bytes > 0:
+                self.gauge_model_weights_memory[engine_idx].set(
+                    scheduler_stats.model_weights_memory_bytes
+                )
+
             self.counter_prefix_cache_queries[engine_idx].inc(
                 scheduler_stats.prefix_cache_stats.queries
             )
diff --git a/vllm/v1/metrics/stats.py b/vllm/v1/metrics/stats.py
index 3956f7e441..ee79f34a07 100644
--- a/vllm/v1/metrics/stats.py
+++ b/vllm/v1/metrics/stats.py
@@ -201,1 +201,3 @@ class SchedulerStats:
+    model_weights_memory_bytes: int = 0
+
     prefix_cache_stats: PrefixCacheStats = field(default_factory=PrefixCacheStats)
diff --git a/vllm/v1/worker/gpu_worker.py b/vllm/v1/worker/gpu_worker.py
index bbab98dbd7..6f72b41484 100644
--- a/vllm/v1/worker/gpu_worker.py
+++ b/vllm/v1/worker/gpu_worker.py
@@ -610,6 +610,12 @@ class Worker(WorkerBase):
             getattr(self.parallel_config, "_api_process_count", 1),
         )

+    def get_model_weights_memory_bytes(self) -> int:
+        """Return bytes of GPU memory consumed by model weights."""
+        # model_memory_usage is set during load_model; default to 0 when the
+        # emulator stubs load_model (VLLM_EMULATOR_ENABLE_ORACLE=1 path).
+        return int(getattr(self.model_runner, "model_memory_usage", 0))
+
     def get_kv_connector_handshake_metadata(
         self,
     ) -> dict[tuple[int, int], KVConnectorHandshakeMetadata] | None:
